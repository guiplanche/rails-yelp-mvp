<h1>Reviews#new</h1>
<p>Find me in app/views/reviews/new.html.erb</p>ff

<div class="container">
  <%= simple_form_for([@restaurant, @review]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: (0..5 ) %>
    <%= f.submit 'Rate!', class: 'btn btn-primary' %>
  <% end %>
</div>
